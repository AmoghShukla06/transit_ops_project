/**
 * Maintenance (mockup #5). Owner: Person B.
 * "Log Service Record" form + service log table. Creating a record flips vehicle -> in_shop.
 * Fetch GET/POST /api/maintenance.
 */
export default function MaintenancePage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Maintenance</h1>
      <p className="text-sm text-muted-foreground">
        TODO(Person B): log-service form + service log table.
      </p>
    </div>
  );
}
