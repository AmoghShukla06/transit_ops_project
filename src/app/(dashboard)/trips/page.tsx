/**
 * Trip Dispatcher (mockup #4). Owner: Person C.
 * Create-trip form with live capacity check (disable Dispatch when cargo > capacity),
 * lifecycle stepper, and the live board. Actions POST /api/trips/{id} { action }.
 */
export default function TripsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Trip Dispatcher</h1>
      <p className="text-sm text-muted-foreground">
        TODO(Person C): create-trip form, capacity validation, lifecycle stepper, live board.
      </p>
    </div>
  );
}
