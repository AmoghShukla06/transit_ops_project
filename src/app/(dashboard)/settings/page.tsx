/**
 * Settings & RBAC (mockup #8). Owner: Person A.
 * General settings (depot, currency, distance unit) + RBAC matrix table.
 * Fetch GET /api/settings.
 */
export default function SettingsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Settings & RBAC</h1>
      <p className="text-sm text-muted-foreground">
        TODO(Person A): general settings form + role-access matrix.
      </p>
    </div>
  );
}
