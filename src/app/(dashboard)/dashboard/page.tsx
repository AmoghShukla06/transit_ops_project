/**
 * Dashboard (mockup #1). Owner: Person C.
 * KPI cards + recent trips + vehicle-status bars + filters (type/status/region).
 * Fetch GET /api/dashboard.
 */
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="text-sm text-muted-foreground">
        TODO(Person C): KPI cards, recent trips, vehicle-status bars, filters.
      </p>
    </div>
  );
}
