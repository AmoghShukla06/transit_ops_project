/**
 * Drivers & Safety Profiles (mockup #3). Owner: Person C.
 * Table + Add/Edit + status toggle + EXPIRED license highlight.
 * Fetch GET /api/drivers.
 */
export default function DriversPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Drivers & Safety</h1>
      <p className="text-sm text-muted-foreground">
        TODO(Person C): driver table, status toggle, license-expiry highlight.
      </p>
    </div>
  );
}
